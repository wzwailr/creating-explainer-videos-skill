# 严格发布质检

Run content/source checks, page/runtime/layout checks, full MP4 decode, media specification probing, black/freeze/silence detection, representative frames, warning-time frames, and a contact sheet.

Then complete muted visual, audio-only, and normal-watch reviews. Automation can only produce a release candidate. A named human must complete all passes before `release_decision: passed`.

